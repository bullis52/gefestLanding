export enum Colors {
  lila = '#e6d7ff',
  blau = '#0000FF',
  gold = '#FFD700',
  braun = '#A52A2A',
  grau = '#808080',
  pink = '#FFC0CB',
  rot = '#FF0000',
  schwarz = '#000000',
  silber = '#C0C0C0',
  transparent = '#FFFFFF00',
  weiß = '#FFFFFF',
}

export enum StatusApi {
  LOADING = 'loading',
  FAILED = 'failed',
  SUCCEEDED = 'succeeded',
}

export enum ReviewStatus {
  IN_PROCESS = 'in_process',
  APPROVED = 'approved',
}

export enum sizeScreens {
  ON_DESKTOP = 1280,
  ON_S_DESKTOP = 1024,
  ON_TABLET = 768,
}

export enum Sort {
  MIN = 'Preis (billigster oben)',
  MAX = 'Preis (teuerster oben)',
}
